{
  "name": "contracts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "hardhat test",
    "build": "hardhat compile",
    "clean": "hardhat clean",
    "typecheck": "tsc --noEmit",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network sepolia",
    "deploy:sepolia:withabi": "npm run deploy:sepolia && npm run sync:abi",
    "verify:sepolia": "hardhat verify --network sepolia",
    "sync:abi": "node scripts/sync-abi.js",
    "build:withabi": "npm run build && npm run sync:abi"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^17.4.1",
    "hardhat": "^2.28.6",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1"
  }
}
