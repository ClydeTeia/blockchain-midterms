# Frontend (TipPost)

This folder contains the React + Vite + TypeScript UI for TipPost.

Main setup and deployment instructions are in the root README:
- `../README.md`

Quick commands:
```bash
npm install
npm run dev
npm run build
npm run lint
```
